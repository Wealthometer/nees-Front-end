# TODO

- [ ] Make hero carousel background responsive on mobile (remove visible black “back background”).
- [x] Update `src/pages/HomePage.jsx` hero wrapper to not force `bg-black` on small screens.
  - [x] Update slide background for `backgroundOnly` to use `bg-cover` (instead of `bg-contain`) on mobile.
- [ ] Verify in browser (desktop + mobile) that no black background is visible behind carousel images.
