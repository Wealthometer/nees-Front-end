# TODO

- [x] Make hero carousel background responsive on mobile (use contain and white background).
- [x] Update `src/pages/HomePage.jsx` hero wrapper to not force `bg-black` on small screens.
  - [x] Update slide background for `backgroundOnly` to use `bg-contain` (instead of `bg-cover`) on mobile with white background.
- [ ] Verify in browser (desktop + mobile) that no black background is visible behind carousel images.
- [x] Reduce white space on mobile devices for a more compact layout.
- [ ] Verify in browser (desktop + mobile) that the layout improvements look good.
